<UserControl x:Class="Desktop.Views.Pages.RatesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:Desktop.Converters"
             Background="#0F0F1E"
             Loaded="OnLoaded">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <Border Grid.Row="0" Padding="24,16" BorderThickness="0,0,0,1" BorderBrush="#1E1E3A">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Tarifas por temporada" Foreground="#666688" FontSize="13" VerticalAlignment="Center"/>

                    <Button Grid.Column="1" Command="{Binding ShowCreateCommand}"
                            Background="#8B5FBF" Foreground="White" BorderThickness="0"
                            Padding="16,8" Cursor="Hand" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Nueva tarifa" Margin="6,0,0,0" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Grid.Column="2" Command="{Binding LoadCommand}"
                            Background="#1A1A3A" BorderThickness="0" Foreground="#8B5FBF"
                            Cursor="Hand" Padding="10,8" ToolTip="Refrescar">
                        <materialDesign:PackIcon Kind="Refresh" Width="18" Height="18"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Season selector -->
            <Border Grid.Row="1" Padding="24,12" BorderThickness="0,0,0,1" BorderBrush="#1E1E3A">
                <StackPanel>
                    <TextBlock Text="Temporada" Foreground="#666688" FontSize="11" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Seasons}"
                              SelectedItem="{Binding SelectedSeason}"
                              DisplayMemberPath="Name"
                              Background="#13132A" Foreground="White" BorderBrush="#2A2A4A" FontSize="13"/>
                </StackPanel>
            </Border>

            <!-- Messages -->
            <StackPanel Grid.Row="2">
                <Border Background="#1A3A1A" Padding="12,8" Margin="16,8,16,0"
                        Visibility="{Binding SuccessMessage, Converter={StaticResource BoolToVis}, ConverterParameter=notempty}">
                    <TextBlock Text="{Binding SuccessMessage}" Foreground="#86EFAC" FontSize="13"/>
                </Border>
                <Border Background="#3D1515" Padding="12,8" Margin="16,8,16,0"
                        Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#FCA5A5" FontSize="13"/>
                </Border>
            </StackPanel>

            <!-- Rates list -->
            <ListView Grid.Row="3" ItemsSource="{Binding Rates}" SelectedItem="{Binding Selected}"
                      Background="Transparent" BorderThickness="0" Margin="16,8"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0,4"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#13132A"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#1A1040"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="12,10" BorderThickness="0,0,0,1" BorderBrush="#1A1A3A">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding RoomTypeName}" Foreground="White" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding PricePerNight, StringFormat='${0:N2}/noche'}"
                                           Foreground="#8B5FBF" FontSize="15" FontWeight="Medium" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Right panel -->
        <Border Grid.Column="1" BorderThickness="1,0,0,0" BorderBrush="#1E1E3A" Background="#0D0D20">
            <Grid>
                <!-- Detail -->
                <Grid Visibility="{Binding ShowDetail, Converter={StaticResource BoolToVis}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="20,16" BorderThickness="0,0,0,1" BorderBrush="#1E1E3A">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Selected.RoomTypeName}" Foreground="White" FontSize="18" FontWeight="Medium"/>
                            <Button Grid.Column="1" Command="{Binding CloseDetailCommand}"
                                    Background="Transparent" BorderThickness="0" Foreground="#666688" Cursor="Hand">
                                <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                            </Button>
                        </Grid>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="20">
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Tipo" Foreground="#666688" FontSize="12"/>
                                <TextBlock Grid.Column="1" Text="{Binding Selected.RoomTypeName}" Foreground="White" FontSize="12"/>
                            </Grid>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Precio/noche" Foreground="#666688" FontSize="12"/>
                                <TextBlock Grid.Column="1" Text="{Binding Selected.PricePerNight, StringFormat='${0:N2}'}" Foreground="#8B5FBF" FontSize="15" FontWeight="Medium"/>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>

                    <Border Grid.Row="2" Padding="20,16" BorderThickness="0,1,0,0" BorderBrush="#1E1E3A">
                        <Button Command="{Binding ShowEditCommand}" Height="40"
                                Background="#1D4ED8" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="13">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Pencil" Width="16" Height="16" VerticalAlignment="Center"/>
                                <TextBlock Text="Editar tarifa" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Border>
                </Grid>

                <!-- Create/Edit form -->
                <Grid Visibility="{Binding ShowCreateForm, Converter={StaticResource BoolToVis}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="20,16" BorderThickness="0,0,0,1" BorderBrush="#1E1E3A">
                        <TextBlock Foreground="White" FontSize="18" FontWeight="Medium">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Nueva tarifa"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                            <Setter Property="Text" Value="Editar tarifa"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="20">
                            <TextBlock Text="Tipo de habitación" Foreground="#666688" FontSize="11" Margin="0,0,0,4"/>
                            <ComboBox ItemsSource="{Binding RoomTypes}"
                                      SelectedIndex="{Binding FormRoomType}"
                                      IsEnabled="{Binding IsEditing, Converter={StaticResource InverseBoolToVisibilityConverter}, FallbackValue=True}"
                                      Background="#13132A" Foreground="White" BorderBrush="#2A2A4A"
                                      FontSize="13" Margin="0,0,0,12"/>

                            <TextBlock Text="Precio por noche ($)" Foreground="#666688" FontSize="11" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding FormPrice, UpdateSourceTrigger=PropertyChanged}"
                                     Background="#13132A" Foreground="White" BorderBrush="#2A2A4A"
                                     Padding="8" FontSize="13"/>
                        </StackPanel>
                    </ScrollViewer>

                    <StackPanel Grid.Row="2" Margin="20,0,20,16">
                        <Button Command="{Binding SaveCommand}" Height="40" Margin="0,4"
                                Background="#8B5FBF" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="13">
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Crear tarifa"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                <Setter Property="Text" Value="Guardar cambios"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Button>
                        <Button Command="{Binding CancelCreateCommand}" Height="36" Margin="0,4"
                                Background="Transparent" Foreground="#666688" BorderThickness="1" BorderBrush="#2A2A4A" Cursor="Hand" FontSize="12">
                            <TextBlock Text="Cancelar"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Loading -->
        <Grid Grid.ColumnSpan="2" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <Grid.Background>
                <SolidColorBrush Color="#80000000"/>
            </Grid.Background>
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                         IsIndeterminate="True" Width="48" Height="48" Foreground="#8B5FBF"
                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>